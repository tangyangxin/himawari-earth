name: Update Himawari Wallpaper


"on":

  workflow_dispatch:

  repository_dispatch:
    types:
      - update


jobs:

  test:

    runs-on: ubuntu-latest

    steps:

      - name: Test
        run: |
          date
          echo "repository dispatch works"
