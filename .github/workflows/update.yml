name: Update Himawari Wallpaper

"on":

  schedule:
    - cron: "*/5 * * * *"

  workflow_dispatch:


jobs:

  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test schedule
        run: |
          date
          echo "workflow works"
